CREATE TABLE users (
	ID INT AUTO_INCREMENT PRIMARY KEY,
    NAME VARCHAR(50),
    EMAIL VARCHAR(100),
    PASSWORD VARCHAR(100),
    ROLE VARCHAR(100),
    STATUS VARCHAR(100),
    CREATED_AT DATE,
    UPDATED_AT DATE
);